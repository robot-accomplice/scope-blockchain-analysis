# Scope Monitor Configuration Schema
# Configuration lives in the `monitor:` section of ~/.config/scope/config.yaml

# ============================================================
# IMPLEMENTED — These fields are active and used by the monitor
# ============================================================

monitor:
  # Layout preset: dashboard | chart-focus | feed | compact
  # Responsive auto-selection picks the best preset for terminal size.
  # Manual switching (L/H keys) overrides auto until A is pressed.
  layout: dashboard

  # Refresh interval in seconds (affects API polling and UI updates)
  refresh_seconds: 10

  # Widget visibility toggles
  # Toggle at runtime with W + digit (1=price_chart, 2=volume, 3=buy_sell, 4=metrics, 5=activity)
  widgets:
    price_chart: true
    volume_chart: true
    buy_sell_pressure: true
    metrics_panel: true
    activity_log: true

# ============================================================
# PLANNED / FUTURE — Not yet implemented; documented for roadmap
# ============================================================

  # Chart configuration (planned)
  # chart:
  #   type: candlestick        # candlestick | line | volume-profile
  #   timeframe: 15m           # 1m | 5m | 15m | 1h | 4h | 1d
  #   scale: linear            # linear | log
  #   show_grid: true
  #   history_points: 100

  # Color scheme (planned): default | protanopia | deuteranopia | monochrome | custom
  # colors: default
  # custom_colors:
  #   up: "#00FF00"
  #   down: "#FF0000"
  #   neutral: "#808080"
  #   background: "#000000"
  #   border: "#333333"
  #   highlight: "#FFFF00"

  # Alert configuration (planned)
  # alerts:
  #   price:
  #     min: null
  #     max: null
  #   whale:
  #     enabled: true
  #     min_usd: 1000
  #   volume_spike:
  #     enabled: true
  #     threshold_percent: 200

  # Transaction feed filters (planned)
  # feed:
  #   min_transaction_usd: 100
  #   max_entries: 50
  #   auto_scroll: true
  #   show_internal: false

  # Data export (planned)
  # export:
  #   enabled: false
  #   format: csv              # csv | json | jsonl
  #   path: ./scope-exports/
  #   filename_template: "{token}_{date}.csv"
  #   fields: [timestamp, price_usd, volume_24h, liquidity_usd, holder_count]

  # Behavior settings (planned)
  # behavior:
  #   auto_pause_on_input: true
  #   confirm_exit: true
  #   show_help_on_startup: false
  #   persist_layout: true

# Keybindings (current defaults, not yet user-configurable)
# keybindings:
#   quit: ["q", "ctrl+c"]
#   pause: ["p", "space"]
#   layout_next: "l"
#   layout_prev: "h"
#   chart_type: "c"
#   timeframe_up: "+"
#   timeframe_down: "-"
#   toggle_widget: "w"
#   auto_layout: "a"
#   help: "?"
